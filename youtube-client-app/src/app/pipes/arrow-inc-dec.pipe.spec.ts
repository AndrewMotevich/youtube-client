import { ArrowIncDecPipe } from './arrow-inc-dec.pipe';

describe('ArrowIncDecPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrowIncDecPipe();
    expect(pipe).toBeTruthy();
  });
});
